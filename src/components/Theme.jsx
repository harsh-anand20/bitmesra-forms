import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#f5ba13',
      light: '#f1ee8e',
      contrastText: '#fff',
    },
  },
});

export default theme;